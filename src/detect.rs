//! Run detection of low coverage region

/* std use */

/* crate use */

/* project use */
use crate::*;

pub fn detect(params: cli::SubCommandDetect) -> error::Result<()> {
    log::debug!("Run detect with params: {:?}", params);

    Ok(())
}
