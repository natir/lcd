//! Run filtration of read with low coverage region or low coverage

/* std use */

/* crate use */

/* project use */
use crate::*;

pub fn filter(params: cli::SubCommandFilter) -> error::Result<()> {
    log::debug!("Run filter with params: {:?}", params);

    Ok(())
}
